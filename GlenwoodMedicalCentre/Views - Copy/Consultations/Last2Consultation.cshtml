@model List<GlenwoodMed.Model.ViewModels.ConsultationView>

<div class="modal-header ">
    Previous Consultation(s)
</div>
<div class="modal-body">

        @foreach (var item in Model)
        {
            <div class="col-md-6">
                <table class="table table-striped table-bordered table-hover table-responsive">
                    <tr class="success">
                        <th>
                            Consultation Date
                        </th>
                        <th>
                            Notes
                        </th>
                        <th>
                            Illness
                        </th>
                        <th>
                            Drugs
                        </th>
                    </tr>
                    <tr>
                        <td>
                            @Html.DisplayFor(model => item.ConsultDate)
                        </td>
                        <td>
                            @Html.DisplayFor(model => item.Notes)
                        </td>
                        <td>
                            @{
                                 foreach (var course in item.Illnesses)
                                 {
                                     @course.Illnessname
                                <br />


                                }
                            }
                        </td>
                        <td>
                            @{
                                foreach (var course in item.Cuts)
                                {
                                    @course.DrugName
                                                    <br />


                                }
                                                }
                        </td>
                    </tr>

                </table>
            </div>
        }

        <hr />
    </div>



